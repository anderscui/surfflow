info:
  name: cluster health
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:9200/_cluster/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
