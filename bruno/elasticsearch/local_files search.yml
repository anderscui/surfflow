info:
  name: local_files search
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:9200/local_files/_search?_source_exclude_vectors=true&size=5
  params:
    - name: _source_exclude_vectors
      value: "true"
      type: query
    - name: size
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
