info:
  name: local_files index - create
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:9200/local_files
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
