info:
  name: indices
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:9200/_cat/indices?format=json
  params:
    - name: format
      value: json
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
