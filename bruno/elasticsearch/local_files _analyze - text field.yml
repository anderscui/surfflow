info:
  name: local_files _analyze - text field
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:9200/local_files/_analyze
  body:
    type: json
    data: |-
      {
        "field": "stem",
        "text": "安妮埃尔诺"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
