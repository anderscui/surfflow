info:
  name: local_files settings
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:9200/local_files/_settings
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
